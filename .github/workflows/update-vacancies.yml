name: Cleanup Duplicate Files

on:
  workflow_dispatch: # Ручной запуск

jobs:
  cleanup:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
          
      - name: Move file from wrong path to correct path
        run: |
          echo "=== ПЕРЕМЕЩЕНИЕ ФАЙЛА В ПРАВИЛЬНОЕ МЕСТО ==="
          
          # Скачиваем файл из неправильного пути
          echo "1. Скачиваем файл из дублирующего пути..."
          curl -v \
            ftp://${{ secrets.FTP_SERVER }}/home/c54971/gradelift.ru/Vacancy/hh_vacancies.json \
            --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
            -o downloaded_hh_vacancies.json || echo "Файл не найден в дублирующем пути"
          
          # Проверяем что файл скачался
          if [ -f "downloaded_hh_vacancies.json" ]; then
            echo "✅ Файл успешно скачан"
            echo "Размер: $(stat -c%s downloaded_hh_vacancies.json) байт"
            echo "Содержимое:"
            head -5 downloaded_hh_vacancies.json
          else
            echo "❌ Файл не найден, создаем новый"
            
            # Создаем новый JSON файл
            python3 << 'EOF'
import requests
import json
from datetime import datetime

try:
    url = "https://api.hh.ru/vacancies"
    headers = {'User-Agent': 'VacancyBot/1.0'}
    params = {
        'text': 'системный администратор',
        'area': '113',
        'per_page': '20'
    }
    
    response = requests.get(url, params=params, headers=headers, timeout=30)
    
    if response.status_code == 200:
        data = response.json()
        items = data.get('items', [])
        
        result = {
            'updated_at': datetime.now().isoformat(),
            'total_count': len(items),
            'status': 'cleanup_and_update',
            'vacancies': []
        }
        
        for item in items:
            pub_date = item.get('published_at', '')
            formatted_date = pub_date
            if pub_date:
                try:
                    dt = datetime.fromisoformat(pub_date.replace('Z', '+00:00'))
                    formatted_date = dt.strftime('%d.%m.%Y %H:%M')
                except:
                    pass
            
            vacancy = {
                'id': item.get('id'),
                'name': item.get('name', ''),
                'company': item.get('employer', {}).get('name', '') if item.get('employer') else '',
                'company_url': item.get('employer', {}).get('alternate_url', '') if item.get('employer') else '',
                'url': item.get('alternate_url', ''),
                'published_at': pub_date,
                'published_date_formatted': formatted_date,
                'area': item.get('area', {}).get('name', '') if item.get('area') else '',
                'salary': item.get('salary'),
                'snippet': item.get('snippet', {}),
                'experience': item.get('experience', {}).get('name', '') if item.get('experience') else '',
                'employment': item.get('employment', {}).get('name', '') if item.get('employment') else '',
                'schedule': item.get('schedule', {}).get('name', '') if item.get('schedule') else ''
            }
            result['vacancies'].append(vacancy)
        
        with open('downloaded_hh_vacancies.json', 'w', encoding='utf-8') as f:
            json.dump(result, f, ensure_ascii=False, indent=2)
        
        print(f"✅ Создан новый файл с {len(items)} вакансиями")
    else:
        print(f"❌ Ошибка API: {response.status_code}")
        
except Exception as e:
    print(f"❌ Ошибка: {e}")
EOF
          fi
          
      - name: Upload to correct path using curl
        run: |
          echo "=== ЗАГРУЗКА В ПРАВИЛЬНЫЙ ПУТЬ ==="
          
          if [ -f "downloaded_hh_vacancies.json" ]; then
            echo "Загружаем файл в правильное место: /Vacancy/"
            
            curl -v -T downloaded_hh_vacancies.json \
              ftp://${{ secrets.FTP_SERVER }}/Vacancy/hh_vacancies.json \
              --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" \
              --ftp-create-dirs
            
            echo "✅ Файл загружен в /Vacancy/"
          else
            echo "❌ Нет файла для загрузки"
          fi
          
      - name: Verify correct placement
        run: |
          echo "=== ПРОВЕРКА ПРАВИЛЬНОГО РАЗМЕЩЕНИЯ ==="
          
          echo "Содержимое папки /Vacancy/:"
          curl -v --list-only \
            ftp://${{ secrets.FTP_SERVER }}/Vacancy/ \
            --user "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" || echo "Папка /Vacancy/ недоступна"
          
          echo ""
          echo "✅ Теперь файл должен быть в правильном месте: /Vacancy/hh_vacancies.json"
          echo "🌐 URL для доступа: https://gradelift.ru/Vacancy/hh_vacancies.json"
