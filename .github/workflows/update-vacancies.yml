name: Update Vacancies

# Запуск каждые 3 часа
on:
  schedule:
    - cron: '0 */3 * * *'  # В 00:00, 03:00, 06:00, 09:00, 12:00, 15:00, 18:00, 21:00 UTC
  
  # Возможность ручного запуска
  workflow_dispatch:

jobs:
  update-vacancies:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
        
    - name: Run vacancy aggregator
      run: |
        python update_vacancies.py
        
    - name: Check if JSON was created
      run: |
        if [ -f "hh_vacancies.json" ]; then
          echo "✅ JSON файл создан успешно"
          echo "📊 Размер файла: $(stat -c%s hh_vacancies.json) байт"
          # Показываем количество вакансий
          python -c "
import json
with open('hh_vacancies.json', 'r', encoding='utf-8') as f:
    data = json.load(f)
print(f'📝 Количество вакансий: {len(data[\"vacancies\"])}')
print(f'🔍 Найдено всего: {data[\"search_stats\"][\"total_found\"]}')
print(f'💾 Сохранено: {data[\"search_stats\"][\"total_saved\"]}')
"
        else
          echo "❌ JSON файл не был создан"
          exit 1
        fi
        
    - name: Upload to FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./
        server-dir: /www/Vacancy/
        include: |
          hh_vacancies.json
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add hh_vacancies.json
        if git diff --staged --quiet; then
          echo "📄 Нет изменений в данных"
        else
          git commit -m "🔄 Обновление вакансий $(date '+%Y-%m-%d %H:%M UTC')"
          git push
          echo "✅ Данные обновлены в репозитории"
        fi
