name: Update Vacancies

on:
  schedule:
    # Запуск каждые 6 часов
    - cron: '0 */6 * * *'
  workflow_dispatch: # Ручной запуск
    inputs:
      force_update:
        description: 'Принудительное обновление'
        required: false
        default: 'false'

jobs:
  update-vacancies:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
          
      - name: Update vacancies data
        run: |
          # Переходим в корневую папку проекта
          cd ${{ github.workspace }}
          
          echo "=== Информация о текущем JSON файле ==="
          if [ -f "hh_vacancies.json" ]; then
            ls -la hh_vacancies.json
            echo "Количество вакансий в старом файле:"
            grep -o '"id"' hh_vacancies.json | wc -l || echo "0"
          else
            echo "Файл hh_vacancies.json не найден"
          fi
          
          # Создаем скрипт для сбора ВСЕХ вакансий
          cat > update_all_vacancies.py << 'EOF'
          import requests
          import json
          import os
          import time
          from datetime import datetime
          
          def collect_all_vacancies():
              """Собирает ВСЕ вакансии со всех страниц"""
              print("=== СБОР ВСЕХ ВАКАНСИЙ СИСТЕМНОГО АДМИНИСТРАТОРА (УДАЛЕНКА) ===")
              print(f"Время начала: {datetime.now()}")
              
              url = "https://api.hh.ru/vacancies"
              headers = {'User-Agent': 'VacancyBot/1.0'}
              
              # ПРАВИЛЬНЫЕ параметры с фильтрами
              params = {
                  'text': 'системный администратор',
                  'area': '113',              # Россия
                  'schedule': 'remote',        # Удаленная работа
                  'search_field': 'name',      # Искать в названии
                  'per_page': '100',           # Максимум на страницу
                  'page': '0'
              }
              
              print(f"📋 Параметры поиска:")
              print(f"   - Текст: {params['text']}")
              print(f"   - Регион: Россия")
              print(f"   - График: Удаленная работа")
              print(f"   - Поиск в: названии вакансии")
              
              all_vacancies = []
              page = 0
              total_pages = None
              
              while True:
                  params['page'] = str(page)
                  
                  try:
                      print(f"\n🔄 Запрос страницы {page + 1}...")
                      response = requests.get(url, params=params, headers=headers, timeout=30)
                      
                      if response.status_code == 200:
                          data = response.json()
                          
                          # На первой странице узнаем общее количество
                          if total_pages is None:
                              total_pages = data.get('pages', 0)
                              total_found = data.get('found', 0)
                              print(f"📊 Найдено всего: {total_found} вакансий")
                              print(f"📄 Страниц для обработки: {total_pages}")
                          
                          items = data.get('items', [])
                          print(f"✅ Получено {len(items)} вакансий на странице {page + 1}")
                          
                          # Обрабатываем вакансии
                          for item in items:
                              vacancy = {
                                  'id': item.get('id'),
                                  'name': item.get('name', ''),
                                  'company': item.get('employer', {}).get('name', '') if item.get('employer') else '',
                                  'company_url': item.get('employer', {}).get('alternate_url', '') if item.get('employer') else '',
                                  'url': item.get('alternate_url', ''),
                                  'published_at': item.get('published_at', ''),
                                  'area': item.get('area', {}).get('name', '') if item.get('area') else '',
                                  'salary': format_salary(item.get('salary')),
                                  'experience': item.get('experience', {}).get('name', '') if item.get('experience') else '',
                                  'employment': item.get('employment', {}).get('name', '') if item.get('employment') else '',
                                  'schedule': item.get('schedule', {}).get('name', '') if item.get('schedule') else '',
                                  'requirement': item.get('snippet', {}).get('requirement', '') if item.get('snippet') else '',
                                  'responsibility': item.get('snippet', {}).get('responsibility', '') if item.get('snippet') else ''
                              }
                              all_vacancies.append(vacancy)
                          
                          # Переход к следующей странице
                          page += 1
                          if page >= total_pages:
                              print(f"\n✅ Обработаны все {total_pages} страниц")
                              break
                          
                          # Задержка между запросами
                          time.sleep(0.5)
                          
                      else:
                          print(f"❌ Ошибка API на странице {page + 1}: {response.status_code}")
                          break
                          
                  except Exception as e:
                      print(f"❌ Ошибка при запросе страницы {page + 1}: {e}")
                      break
              
              return all_vacancies
          
          def format_salary(salary_data):
              """Форматирует информацию о зарплате"""
              if not salary_data:
                  return "не указана"
              
              salary_from = salary_data.get('from')
              salary_to = salary_data.get('to')
              currency = salary_data.get('currency', 'RUR')
              gross = salary_data.get('gross', False)
              
              if salary_from and salary_to:
                  result = f"от {salary_from:,} до {salary_to:,} {currency}".replace(',', ' ')
              elif salary_from:
                  result = f"от {salary_from:,} {currency}".replace(',', ' ')
              elif salary_to:
                  result = f"до {salary_to:,} {currency}".replace(',', ' ')
              else:
                  return "не указана"
              
              if gross:
                  result += " (до вычета налогов)"
              else:
                  result += " (на руки)"
              
              return result
          
          def main():
              # Удаляем старый файл
              filename = 'hh_vacancies.json'
              if os.path.exists(filename):
                  os.remove(filename)
                  print(f"✅ Старый файл удален")
              
              # Собираем все вакансии
              vacancies = collect_all_vacancies()
              
              print(f"\n📊 ИТОГО собрано вакансий: {len(vacancies)}")
              
              # Создаем результат
              result = {
                  'source': 'hh.ru',
                  'search_params': {
                      'text': 'системный администратор',
                      'area': 'Россия',
                      'schedule': 'Удалённая работа',
                      'search_field': 'В названии вакансии'
                  },
                  'updated': datetime.now().strftime('%Y-%m-%dT%H:%M:%SZ'),
                  'total_count': len(vacancies),
                  'vacancies': vacancies
              }
              
              # Сохраняем файл
              with open(filename, 'w', encoding='utf-8') as f:
                  json.dump(result, f, ensure_ascii=False, indent=2)
              
              print(f"✅ Файл {filename} создан")
              print(f"📁 Размер файла: {os.path.getsize(filename)} байт")
              
              # Выводим статистику
              if vacancies:
                  companies = {}
                  for v in vacancies:
                      company = v['company']
                      if company:
                          companies[company] = companies.get(company, 0) + 1
                  
                  top_companies = sorted(companies.items(), key=lambda x: x[1], reverse=True)[:5]
                  print("\n🏢 Топ-5 компаний:")
                  for company, count in top_companies:
                      print(f"   - {company}: {count} вакансий")
              
              return len(vacancies) > 0
          
          if __name__ == "__main__":
              success = main()
              print(f"\n🏁 Результат: {'УСПЕХ' if success else 'ОШИБКА'}")
              exit(0 if success else 1)
          EOF
          
          # Запускаем скрипт
          python update_all_vacancies.py
          
          echo ""
          echo "=== Информация о файле после обновления ==="
          if [ -f "hh_vacancies.json" ]; then
            ls -la hh_vacancies.json
            echo "Количество вакансий в новом файле:"
            grep -o '"id"' hh_vacancies.json | wc -l || echo "0"
            echo ""
            echo "Первые 3 вакансии:"
            head -50 hh_vacancies.json | grep -E '"name"|"company"' | head -6
          else
            echo "ОШИБКА: Файл hh_vacancies.json не был создан!"
          fi
          
      - name: Commit and push changes
        run: |
          cd ${{ github.workspace }}
          
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          git add hh_vacancies.json
          
          if git diff --staged --quiet; then
            echo "⚠️ Нет изменений для коммита"
          else
            VACANCY_COUNT=$(grep -o '"id"' hh_vacancies.json | wc -l || echo "0")
            git commit -m "🔄 Обновление вакансий: найдено $VACANCY_COUNT вакансий $(date '+%Y-%m-%d %H:%M:%S')"
            git push
          fi
          
      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: "./"
          server-dir: "www/Vacancy/"
          state-name: ".ftp-deploy-sync-state.json"
          dry-run: false
          log-level: verbose
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/.github/**
            **/README.md
            **/update_vacancies.py
            **/update_all_vacancies.py
            **/simple_update.py
            
      - name: Verify deployment
        run: |
          echo "=== Проверка успешности деплоя ==="
          echo "Количество вакансий в файле:"
          grep -o '"id"' hh_vacancies.json | wc -l || echo "0"
          echo ""
          echo "Дата обновления:"
          grep -o '"updated":[^,]*' hh_vacancies.json || echo "updated не найден"
          echo ""
          echo "✅ Деплой завершен. Проверьте файл на сервере."
